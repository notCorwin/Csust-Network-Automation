## 软件预期的行为

1. 自动通过 login.csust.edu.cn 登录校园网（SSID：CSUST-Student）。
2. 不尝试登录非校园网；监测到切换到校园网且未登录时，立即登录校园网。
3. 如果登录不成功，在获取到结果后立即重试，直到成功。
   1. 可以在特殊情况下不重试，如账号密码错误。
4. 在成功登录校园网后保持轮询，在失去与互联网的连接时，自动重新登录。
   1. 连通证据包括 Apple，Google 和校园网。
5. 自动兼容机器的代理环境（除非本机网络设置有问题）。
6. 尽可能避免中间人/恶意双胞胎攻击的风险，同时保证能够自动登录。
7. 每隔 3 分钟通过 GitHub Releases 获取一次软件的最新版本。
8. 随开机自动启动，并保持后台运行。
9. 下拉菜单保持实时刷新。
10. 让 Dock icon 随 app 的 window lifecycle 动态显隐，只有实际 UI 打开时才进入 Dock。
11. 保持菜单栏图标的 ID 稳定。
12. 需要弹窗时，记得要自动切换焦点。


## 菜单栏 SF Symbols 动画组件

1. 已连接到互联网：`network`
   1. 无论是否通过校园网连接，只要能连上网就显示这个。

2. 未连接到互联网：`network.slash`
   1. 动画：替换（连续）
   2. 动画效果：按图层
   3. 方向：向上 - 向上
   4. 不使用魔术替换

## 开发要求

1. 通过 GitHub Actions 和 Releases 实现检查更新功能。
2. 每次做出改动后，自动提交并推送。
3. 只使用 AppKit/UIKit App，弃用 SwiftUI。
4. 弃用 Keychain。
5. 设置页只设置账号和密码，其他都写进代码里。
